prefix_rule(
    pattern = ["git", "add"],
    decision = "allow",
    justification = "Allow staging repository changes",
)

prefix_rule(
    pattern = ["git", "commit"],
    decision = "allow",
    justification = "Allow creating git commits in this repository",
)
